# ─── Training Configuration (Semantic Only) ───
training:
  seed: 42
  device: "auto"
  num_workers: 0
  pin_memory: true

  optimizer: "adamw"
  learning_rate: 2.0e-5
  weight_decay: 0.01
  max_grad_norm: 1.0
  warmup_ratio: 0.1

  scheduler: "cosine"
  epochs: 10
  batch_size: 8
  gradient_accumulation_steps: 4

  early_stopping:
    enabled: true
    patience: 3
    metric: "val_f1_binary"
    mode: "max"

  checkpoint:
    save_dir: "models/checkpoints"
    save_top_k: 1
    save_every_n_epochs: 0

  loss_weights:
    binary: 1.0
    cwe: 0.5
    severity: 0.2

  quality_tiers:
    gold: 1.0
    silver: 0.85

  logging:
    backend: "tensorboard"
    log_dir: "runs/"
    log_every_n_steps: 50
    wandb_project: "vulhunter"
